# Claude Config

A collection of reusable Claude Code configurations including agents, commands, skills, and other customizations.

## Structure

- `agents/` - Custom agent definitions for specialized tasks
- `commands/` - Reusable slash commands
- `skills/` - Custom skills for Claude Code

## Usage

These configurations can be shared across projects by symlinking or copying to your project's `.claude/` directory.
